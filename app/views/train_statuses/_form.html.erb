<%= form_with(model: train_status, local: true) do |form| %>
  <% if train_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(train_status.errors.count, "error") %> prohibited this train_status from being saved:</h2>

      <ul>
      <% train_status.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

<%= form_with(model: text_message, local: true) do |form| %>
  <% if text_message.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(text_message.errors.count, "error") %> prohibited this text_message from being saved:</h2>

      <ul>
      <% text_message.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :to %>
    <%= form.text_field :to %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
